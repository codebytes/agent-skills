paths:
  skills: plugins/document-tools/skills/
  evals: evals/
  results: evals/.results/

defaults:
  engine: mock
  model: mock-model
  timeout: 30
  parallel: false
  verbose: false
  sessionLog: false

cache:
  enabled: false

tokens:
  warningThreshold: 500
  fallbackLimit: 5000
